# App Layer

Keep the app shell here:

- router setup
- main layout
- page-level composition
- route guards if needed later

If you use Vite React, this will usually hold:

- `App.tsx`
- route definitions
- layout wrappers
